amcl:
  ros__parameters:
    use_sim_time: true

    # 🔑 FIX IS HERE
    base_frame_id: "base_link"
    odom_frame_id: "odom"
    global_frame_id: "map"

    scan_topic: "scan"

    # Reasonable defaults
    min_particles: 500
    max_particles: 2000
    update_min_d: 0.25
    update_min_a: 0.2
    laser_max_range: 12.0
